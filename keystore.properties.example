# release 签名配置模板
# 复制为 keystore.properties 后填真实值。keystore.properties 已在 .gitignore 中，不会入库。
# storeFile 可写绝对路径，也可写相对项目根目录的路径。

storeFile=C:/path/to/water_keystore.jks
storePassword=密钥库口令
keyAlias=密钥别名
keyPassword=密钥口令
